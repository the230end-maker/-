import { ButtonHTMLAttributes, useRef } from 'react';
export function MagneticButton({className='',children,...props}:ButtonHTMLAttributes<HTMLButtonElement>){ const ref=useRef<HTMLButtonElement>(null); return <button ref={ref} onMouseMove={(e)=>{const r=ref.current!.getBoundingClientRect(); ref.current!.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`}} onMouseLeave={()=>{ref.current!.style.transform='translate(0,0)'}} className={`ripple glow transition-all border border-white/15 bg-white/5 px-4 py-2 rounded-[10px] ${className}`} {...props}>{children}</button> }
