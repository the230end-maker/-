export default function Timeline({items=[]}:{items:any[]}){ return <div className="glass p-5"><h2 className="font-display text-2xl mb-4">الخط الزمني</h2><div className="border-r border-white/20 pr-6">{items.map((e:any,i)=><div draggable key={e.id||i} className="relative mb-6 hover:translate-x-1 transition"><span className="absolute -right-[31px] top-1 h-3 w-3 rounded-full bg-white shadow-[0_0_20px_#fff8]"/><b>{e.event}</b><p className="text-white/50 text-sm">الفصل {e.chapter} — التأثير: {e.impact}</p></div>)}<button>+ إضافة حدث</button></div></div> }
