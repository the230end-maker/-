import { UserRound, Lock } from 'lucide-react'; import { useNovelStore } from '../../store/novelStore';
export default function CharacterCard({character}:{character:any}){ const {saveDoc}=useNovelStore(); const upd=(k:string,v:string)=>saveDoc('characters',{...character,[k]:v}); return <div className="glass glow p-4 fade-stagger"><UserRound className="mb-3"/><input className="font-display text-2xl bg-transparent outline-none w-full" value={character.name||''} onChange={e=>upd('name',e.target.value)}/><hr className="border-white/10 my-3"/><p>الدور: <input className="bg-transparent" value={character.role||''} onChange={e=>upd('role',e.target.value)}/></p><p>العمر: <input className="bg-transparent w-16" value={character.age||''} onChange={e=>upd('age',e.target.value)}/> الجنس: <input className="bg-transparent w-20" value={character.gender||''} onChange={e=>upd('gender',e.target.value)}/></p>{['appearance','personality','motivations','secrets'].map(k=><textarea key={k} className="typing-caret mt-2 w-full bg-white/[.02] border border-white/10 rounded-[8px] p-2" placeholder={k==='secrets'?'الأسرار 🔒':k} value={character[k]||''} onChange={e=>upd(k,e.target.value)}/>) }<p className="text-xs text-white/50 mt-2">تطور في الفصل: {(character.chapterAppearances||[]).join(', ')}</p><button className="mt-2 text-sm underline">عرض في السياق</button></div> }
