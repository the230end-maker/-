import { motion } from 'framer-motion'; import { MagneticButton } from '../ui/MagneticButton';
export default function ConflictPanel({a,b,onClose}:any){ if(!a||!b) return null; return <motion.div className="fixed inset-0 bg-black/70 grid place-items-center z-50" initial={{opacity:0}} animate={{opacity:1}}><motion.div className="glass max-w-3xl p-6" initial={{scale:.9,y:30}} animate={{scale:1,y:0}}><h2 className="font-display text-3xl mb-4">⚠️ نقطة خلاف</h2><div className="grid grid-cols-2 gap-4"><div className="glass p-4"><b>{a.agent}</b><p>{a.reason}</p></div><div className="glass p-4"><b>{b.agent}</b><p>{b.reason}</p></div></div><div className="flex gap-3 mt-5"><MagneticButton onClick={onClose}>اختر A</MagneticButton><MagneticButton onClick={onClose}>اختر B</MagneticButton><MagneticButton onClick={onClose}>دع الفريق يقرر</MagneticButton></div></motion.div></motion.div> }
