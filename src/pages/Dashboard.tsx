import { motion } from 'framer-motion'; import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useNovelStore } from '../store/novelStore'; import { MagneticButton } from '../components/ui/MagneticButton';
export default function Dashboard(){ const {state,createNovel}=useNovelStore(); const nav=useNavigate(); const [form,setForm]=useState({title:'',genre:'فانتازيا',initialIdea:''}); async function start(){ await createNovel(form); nav('/studio'); } return <main className="min-h-[calc(100vh-104px)] grid place-items-center p-8"><motion.div className="glass gradient-border p-8 max-w-3xl w-full" initial={{opacity:0,y:20}} animate={{opacity:1,y:0}}><h1 className="font-display text-7xl mb-2">اكتب روايتك</h1><p className="text-white/50 mb-6">منصة كتابة إبداعية بذاكرة دائمة وفريق عملاء DeepSeek.</p>{state?.novel&&<div className="glass p-4 mb-6">رواية موجودة: <b>{state.novel.title}</b> — آخر تعديل {state.novel.lastModified}</div>}<input className="typing-caret w-full mb-3 bg-white/5 border border-white/10 rounded-[10px] p-3" placeholder="اسم الرواية" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><select className="w-full mb-3 bg-[#111] border border-white/10 rounded-[10px] p-3" value={form.genre} onChange={e=>setForm({...form,genre:e.target.value})}>{['خيال علمي','فانتازيا','رومانسي','تشويق','غموض','دراما'].map(g=><option key={g}>{g}</option>)}</select><textarea className="typing-caret w-full h-32 mb-4 bg-white/5 border border-white/10 rounded-[10px] p-3" placeholder="ملخص الفكرة الأولية" value={form.initialIdea} onChange={e=>setForm({...form,initialIdea:e.target.value})}/><MagneticButton onClick={start}>أطلق الفريق الإبداعي</MagneticButton></motion.div></main> }
